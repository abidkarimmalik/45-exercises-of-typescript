// Define an array
let errornumbers: number[]= [1, 2, 3, 4, 5];

// Attempt to access an index that doesn't exist
let index = 5; // Change this index to an invalid index

  //  console.log(errornumbers[5]);

  console.log(errornumbers[3]);
  //error is removed
